export class advice {
  generalPopulation: string
  sensitivePopulation: string

  constructor(generalPopulation: string, sensitivePopulation: string) {
    this.generalPopulation = generalPopulation
    this.sensitivePopulation = sensitivePopulation
  }
}