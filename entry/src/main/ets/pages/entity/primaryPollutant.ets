export class primaryPollutant {
  code: string
  name: string
  fullName: string

  constructor(code: string, name: string, fullName: string) {
    this.code = code
    this.name = name
    this.fullName = fullName
  }
}